// PLACEHOLDER hashed asset. Stands in for a real Vite content-hashed bundle
// (assets/index-<hash>.js) so TestEmbed can assert the immutable Cache-Control header on a path that
// is NOT index.html. `make build` overwrites internal/ui/dist with the real output, replacing this.
export const placeholder = true;

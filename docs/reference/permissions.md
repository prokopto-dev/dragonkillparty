<!-- GENERATED by `make gen` from internal/authz/catalogue.go. Edit the catalogue, not this file. -->

# Permissions

> **NOT YET ENFORCED.** This describes the contract Phase 2 implements, not what the server checks today: there is no authentication or authorization middleware yet, so every operation is served without a credential — including the mutating `PATCH /api/v1/guild`. See "Known Phase 0 gaps" in SECURITY.md.

The 58 permission keys a role can grant. They are `<resource>.<action>`, dot-separated and
lowercase, and this page is generated from `internal/authz/catalogue.go` — the single source
([canonical §6](../design/00-canonical-conventions.md)). The same catalogue is reconciled
into the `permission` table at every boot, so a key here is a key the database has a row
for.

Two values appear in an operation's permission column and are **not** keys here:
`public` (no credential required) and `self` (any authenticated principal, constrained to its
own records).

A **step-up** key is in [the capability floor](#the-capability-floor): session-only,
no PAT scope, and a recent re-authentication. A **dangerous** key is one the role editor
confirms twice before granting — an affordance, not a control.

## Roster

| Key | Label | Flags | Description |
|---|---|---|---|
| `roster.read` | Read roster |  | View the guild roster, members and characters. |
| `roster.write` | Edit roster |  | Add, edit and archive members and characters. |
| `person.merge` | Merge people |  | Merge two person records into one, re-parenting their characters. |
| `character.claim.approve` | Approve character claims |  | Approve or reject a member's claim to a character. |
| `character.key.verify` | Verify zone keys |  | Move a self-reported zone key to verified, which gates raid eligibility. |

## Raids

| Key | Label | Flags | Description |
|---|---|---|---|
| `raid.read` | Read raids |  | View raids, attendance and ticks. |
| `raid.create` | Create raids |  | Open a new raid. |
| `raid.update` | Update raids |  | Edit a raid's details before it is finalized. |
| `raid.finalize` | Finalize raids |  | Finalize a raid, freezing its attendance and ticks. |
| `raid.tick.create` | Create ticks |  | Award an attendance tick during a raid. |
| `raid.tick.delete` | Delete ticks |  | Remove an attendance tick from a raid. |
| `raid.custody.manage` | Manage custody |  | Transfer custody of an in-progress raid between officers. |

## Loot

| Key | Label | Flags | Description |
|---|---|---|---|
| `item.read` | Read items |  | View the item catalogue and award history. |
| `item.award` | Award items |  | Record an item award to a character. |
| `item.alias.manage` | Manage item aliases |  | Add and edit the aliases the parser resolves item names through. |
| `item.priority.manage` | Manage item priority |  | Edit the loot-priority lists that resolution reads. |

## Points

| Key | Label | Flags | Description |
|---|---|---|---|
| `dkp.read` | Read points |  | View standings, balances and the ledger. |
| `dkp.adjust` | Adjust points |  | Create a manual point adjustment. |
| `dkp.decay.run` | Run decay |  | Run a decay pass over the point pools. |
| `ledger.reverse` | Reverse a batch |  | Write a reversal batch that undoes a prior ledger batch. |

## Bidding

| Key | Label | Flags | Description |
|---|---|---|---|
| `bid.read` | Read bids |  | View bid sessions and, after reveal, the bids in them. |
| `bid.manage` | Manage bids |  | Open, extend, close, resolve and settle bid sessions. |
| `bid.reveal_early` | Reveal bids early | **dangerous** | Reveal a sealed bid session before its scheduled reveal. |

## Bank

| Key | Label | Flags | Description |
|---|---|---|---|
| `bank.read` | Read bank |  | View the guild bank's contents and requests. |
| `bank.request` | Request from bank |  | Ask the guild bank for an item. |
| `bank.fulfil` | Fulfil bank requests |  | Hand over a requested item and close the delivery handshake. |
| `bank.manage` | Manage bank |  | Add, remove and reconcile the guild bank's inventory. |

## Calendar

| Key | Label | Flags | Description |
|---|---|---|---|
| `calendar.read` | Read calendar |  | View the raid calendar and events. |
| `calendar.write` | Edit calendar |  | Create and edit calendar events. |
| `signup.manage` | Manage signups |  | Manage the signup list for an event. |

## Draft

| Key | Label | Flags | Description |
|---|---|---|---|
| `draft.read` | Read drafts |  | View draft weeks and ballots. |
| `draft.vote` | Vote in a draft |  | Submit a ranked ballot in an open draft. |
| `draft.manage` | Manage drafts |  | Open, close and publish draft weeks and quakes. |

## Swaps

| Key | Label | Flags | Description |
|---|---|---|---|
| `swap.request` | Request a swap |  | Ask for a main-character swap quote. |
| `swap.approve` | Approve a swap |  | Approve or deny a requested main swap. |
| `swap.policy.manage` | Manage swap policy |  | Edit the pricing and rules that govern main swaps. |

## Recruitment

| Key | Label | Flags | Description |
|---|---|---|---|
| `recruit.read` | Read applications |  | View recruitment applications. |
| `recruit.comment` | Comment on applications |  | Leave signed feedback on a recruitment application. |
| `recruit.decide` | Decide on applications |  | Accept or reject a recruitment application. |
| `vouch.manage` | Manage vouches |  | Record and settle vouches for applicants. |

## Policy

| Key | Label | Flags | Description |
|---|---|---|---|
| `policy.read` | Read policies |  | View guild policies and their acceptance state. |
| `policy.write` | Edit policies |  | Author and revise guild policies. |

## Content

| Key | Label | Flags | Description |
|---|---|---|---|
| `cms.read` | Read content |  | View articles, media and portal blocks. |
| `cms.write` | Edit content |  | Author and edit articles, media and portal blocks. |
| `cms.moderate` | Moderate content |  | Moderate comments and untrusted rich text. |

## Import

| Key | Label | Flags | Description |
|---|---|---|---|
| `import.run` | Run an import |  | Run an EQdkp import in dry-run, staging the data without committing it. |
| `import.commit` | Commit an import | **step-up** | Commit a staged import into the live database. |

## Webhooks

| Key | Label | Flags | Description |
|---|---|---|---|
| `webhook.manage` | Manage webhooks |  | Create, edit and delete outbound webhooks. |

## Tokens

| Key | Label | Flags | Description |
|---|---|---|---|
| `token.mint` | Mint tokens | **step-up** | Mint a personal access token or a service-account token. |
| `token.revoke` | Revoke tokens | **step-up** | Revoke a personal access token or a service-account token. |

## Administration

| Key | Label | Flags | Description |
|---|---|---|---|
| `admin.settings` | Manage settings |  | Change instance configuration that does not affect the security posture, such as the guild name, timezone and point label. |
| `admin.security.manage` | Manage security settings | **step-up** | Read and change security-affecting configuration — identity-provider credentials, MFA and session policy, the outbound allowlist — and feed tokens. |
| `admin.roles.manage` | Manage roles | **step-up** | Edit roles and role assignments. |
| `admin.backup` | Manage backups | **step-up** | Download and restore database backups. |
| `admin.owner` | Owner | **step-up** | The owner capability: an ordinary permission row, not a hardcoded superuser. |

## Sensitive

| Key | Label | Flags | Description |
|---|---|---|---|
| `person.pii.read` | Read PII | **step-up** | Read personally-identifying information in bulk, such as email addresses. |
| `audit.read` | Read the audit log | **step-up** | Read the audit log. |
| `ops.read` | Read operational status |  | Read operational status and diagnostics. |

## The capability floor

These keys alter authentication, authorization or bulk-export state. Operations that
require one are **session and step-up only** and carry no PAT scope at all — there is no
`admin:*` scope and no all-powerful token ([ADR-0011](../adr/0011-opaque-pats-no-superadmin-token.md)).
Holding one is an ordinary role grant; exercising it needs a re-authenticated browser session.

- `token.mint`
- `token.revoke`
- `admin.security.manage`
- `admin.roles.manage`
- `admin.backup`
- `admin.owner`
- `person.pii.read`
- `audit.read`
- `import.commit`

`admin.owner` is in that list and is an **ordinary permission row**, not a hardcoded
superuser branch. Revoking it is an ordinary role change, and at least one account must hold it.
